apiVersion: v1
kind: Service
metadata:
  name: eldocam-frontend
  namespace: eldocam
  labels:
    app: eldocam-frontend  # ← Cohérent partout
spec:
  selector:
    app: eldocam-frontend  # ← CORRECTION ICI
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 80
  type: ClusterIP
